# UniCore releases

Public UniCore release artifacts are published in this shared hub with tags prefixed
by `unicore-`, for example `unicore-v1.0.0`.

Stable update metadata lives in:

`channels/unicore/stable.json`

The private source of truth is `Jojje84/UniCore`.
