# ForgeCore releases

Public ForgeCore release artifacts are published in this shared hub with tags prefixed
by `forgecore-`, for example `forgecore-v0.1.0-beta.20`.

Stable update metadata lives in:

`channels/forgecore/stable.json`

The private source of truth is `Jojje84/ForgeCore`.
